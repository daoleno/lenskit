import { createStitches } from '@stitches/react'
const { styled } = createStitches({
  theme: {
    colors: {
      lime: '#ABFE2C',
      basil: '#00501E',
      peas: '#E5FFBE',
    },
  },
})

export default styled
